@model IEnumerable<websitestintuc.Models.category>
    @foreach(var item in Model)
    {
        <a class="contentCategory" href="@Url.Action("getNewsCategories", "Main", new { id = item.ID })">
            @item.CategoryName
        </a>
    }
